<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">

    <style>
        body {
            font-family: Helvetica, Arial, sans-serif;
            font-size: 14px;
            color: #000;
        }

        .title {
            text-align: center;
            font-size: 22px;
            margin-bottom: 25px;
        }

        .film {
            display: flex;
            gap: 20px;
            margin-bottom: 20px;
        }

        img {
            width: 150px;
            height: auto;
        }

        .box {
            margin-top: 20px;
            border: 1px solid #000;
            padding: 15px;
        }


    </style>
</head>

<body>

<h1 class="title">Reçu de réservation</h1>

<div class="film">
    <img src="{{ absolute_url(asset('uploads/images/' ~ film.imageFilename)) }}" alt="{{ film.title }}">

    <div>
        <p><strong>Film :</strong> {{ film.title }}</p>
        <p><strong>Date :</strong> {{ date }}</p>
        <p><strong>Heure :</strong> {{ time }}</p>
        <p><strong>Salle :</strong> {{ salle }}</p>
    </div>
</div>

<div class="box">
    <p><strong>Client :</strong> {{ user.nom }} {{ user.prenom }}</p>
    <p><strong>Email :</strong> {{ user.email }}</p>
    <p><strong>Tarif :</strong> <span id="totalPrice">0</span> DH</p>

</div>

<p style="margin-top:30px;">
    Merci pour votre réservation 🎬
</p>

</body>
</html>
